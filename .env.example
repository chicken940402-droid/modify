# Copy to `.env` and fill with your Supabase settings.
# Keep DATABASE_URL / DIRECT_URL server-side only. PUBLIC_* can be used by the browser.

DATABASE_URL="postgresql://postgres.<project-ref>:password@aws-1-<region>.pooler.supabase.com:6543/postgres?pgbouncer=true&connection_limit=1"
DIRECT_URL="postgresql://postgres.<project-ref>:password@aws-1-<region>.pooler.supabase.com:5432/postgres?connection_limit=1"
PUBLIC_SUPABASE_URL="https://<project>.supabase.co"
PUBLIC_SUPABASE_PUBLISHABLE_KEY="sb_publishable_xxx"
